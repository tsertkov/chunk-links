{
  "name": "chunk-links",
  "version": "0.1.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "build": "rm -rf dist/*; babel -d dist src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash.debounce": "^4.0.8"
  },
  "devDependencies": {}
}
